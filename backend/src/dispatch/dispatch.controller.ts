import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { Role } from '@prisma/client'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { AuthUser } from '../common/auth-user'; import { DispatchService } from './dispatch.service'; import { CreateDeliveryDto, CreateDispatchDto, CreatePackingDto } from './dto/dispatch.dto';
@Controller('dispatch') @UseGuards(AuthGuard('jwt'),RolesGuard) @Roles(Role.SUPERADMIN,Role.MANAGER) export class DispatchController {constructor(private s:DispatchService){} @Get() list(){return this.s.list()} @Post('packing') pack(@Body() d:CreatePackingDto){return this.s.pack(d)} @Post() dispatch(@Body() d:CreateDispatchDto,@CurrentUser() u:AuthUser){return this.s.dispatch(d,u)} @Post(':id/delivery') deliver(@Param('id') id:string,@Body() d:CreateDeliveryDto,@CurrentUser() u:AuthUser){return this.s.deliver(id,d,u)} }
