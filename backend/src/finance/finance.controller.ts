import { Body, Controller, Get, Post, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { Role } from '@prisma/client'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { AuthUser } from '../common/auth-user'; import { FinanceService } from './finance.service'; import { ExpenseDto, PaymentDto } from './dto/finance.dto'; @Controller('finance') @UseGuards(AuthGuard('jwt'),RolesGuard) @Roles(Role.SUPERADMIN,Role.MANAGER) export class FinanceController {constructor(private s:FinanceService){} @Get('summary') summary(){return this.s.summary()} @Get('expenses') expenses(){return this.s.expenses()} @Post('expenses') add(@Body() d:ExpenseDto,@CurrentUser() u:AuthUser){return this.s.addExpense(d,u.id)} @Get('payments') payments(){return this.s.payments()} @Post('payments') pay(@Body() d:PaymentDto){return this.s.pay(d)} }
