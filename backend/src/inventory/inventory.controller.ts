import { Controller, Get, Query, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { Role, StockCategory } from '@prisma/client'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { InventoryService } from './inventory.service';
@Controller('inventory') @UseGuards(AuthGuard('jwt'),RolesGuard) @Roles(Role.SUPERADMIN,Role.MANAGER) export class InventoryController {constructor(private s:InventoryService){} @Get('balances') balances(@Query('category') c:StockCategory,@Query('itemCode') i?:string,@Query('lotNo') l?:string){return this.s.balance(c,i,l)} @Get('ledger') ledger(@Query('category') c?:StockCategory,@Query('itemCode') i?:string,@Query('lotNo') l?:string){return this.s.ledger({category:c,itemCode:i,lotNo:l})}}
