import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { Role } from '@prisma/client'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { OrdersService } from './orders.service'; import { CreateOrderDto, CreateProductionOrderDto } from './dto/create-order.dto';
@Controller('orders') @UseGuards(AuthGuard('jwt'),RolesGuard) export class OrdersController {constructor(private s:OrdersService){} @Get() @Roles(Role.SUPERADMIN,Role.MANAGER) list(){return this.s.list()} @Post() @Roles(Role.SUPERADMIN,Role.MANAGER) create(@Body() d:CreateOrderDto){return this.s.create(d)} @Get(':id') @Roles(Role.SUPERADMIN,Role.MANAGER) get(@Param('id') id:string){return this.s.get(id)} @Post(':id/production-orders') @Roles(Role.SUPERADMIN,Role.MANAGER) prod(@Param('id') id:string,@Body() d:CreateProductionOrderDto){return this.s.createProductionOrder(id,d)} @Get(':id/traceability') @Roles(Role.SUPERADMIN,Role.MANAGER) trace(@Param('id') id:string){return this.s.trace(id)} }
