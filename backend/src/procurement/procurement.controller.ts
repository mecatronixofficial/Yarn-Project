import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { Role } from '@prisma/client'; import { CurrentUser } from '../common/decorators/current-user.decorator'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { AuthUser } from '../common/auth-user'; import { ProcurementService, PurchaseInput, ReceiveInput } from './procurement.service'; @Controller('procurement') @UseGuards(AuthGuard('jwt'),RolesGuard) @Roles(Role.SUPERADMIN,Role.MANAGER) export class ProcurementController {constructor(private s:ProcurementService){} @Get('purchase-orders') list(){return this.s.list()} @Post('purchase-orders') create(@Body() d:PurchaseInput){return this.s.create(d)} @Post('purchase-orders/:id/receive') receive(@Param('id') id:string,@Body() d:ReceiveInput,@CurrentUser() u:AuthUser){return this.s.receive(id,d,u)} }
